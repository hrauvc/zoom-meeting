name: Tests

on:
  push:
    branches: [ developer ]
  pull_request:
    branches: [ developer ]

jobs: 
  test:
    runs-on: ubuntu-latest
    steps: 
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'

      - name: Install dependencies
        run: composer install --no-interaction

      - name: Request Zoom Credentials
        run: |
          read -p "Zoom account: " zoom_account
          read -p "Zoom client: " zoom_client
          read -p "Zoom secret: " zoom_secret
          read -p "Meeting ID: " zoom_meeting_id
          echo "::set-env name=ZOOM_ACCOUNT::$zoom_account"
          echo "::set-env name=ZOOM_CLIENT::$zoom_client"
          echo "::set-env name=ZOOM_SECRET::$zoom_secret"
          echo "::set-env name=ZOOM_MEETING_ID::$zoom_meeting_id"
      - name: Run tests
        run: vendor/bin/phpunit